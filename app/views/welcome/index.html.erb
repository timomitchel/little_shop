

<section class="center">
  <section class="category_links">
    <%= link_to 'all items', items_path %>
    <% @categories.each do |category| %> 
      <%= link_to "#{category.title}", category_path(category.title) %>
    <% end %> 
  </section>
 </section>
